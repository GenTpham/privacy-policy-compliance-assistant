FROM node:20-alpine

WORKDIR /app

# Copy dependency manifests and install
COPY package*.json ./
RUN npm install

# Command to run Vite dev server exposed to all interfaces
CMD ["npm", "run", "dev", "--", "--host"]
